using OAuthLogin.DAL.ViewModels;

namespace OAuthLogin.BLL.Repositories
{
    internal interface IAuthService
    {
        //GoogleLogin([FromBody] VMGoogleSignin model)

        //Login([FromBody] VMLogin model)

        // Register([FromBody] VMRegister model)

        //ConfirmEmail([FromQuery] VMConfirmEmail model)

        //ConfirmAddUserEmail([FromQuery] VMAddConfirmEmail model)

        // ForgotPassword([FromBody] VMForgotPassword model)

        //ResetPassword([FromBody] VMResetPassword model)
    }
}
